#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.dtsi>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &none &none
            >;
        };
    };
};
